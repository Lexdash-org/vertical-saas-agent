{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "allowJs": true
  },
  "_comment": [
    "`include` is a bare glob on purpose. This file is copied verbatim into an installed",
    "skill, whose layout is <skill>/references and <skill>/scripts — not the repo's",
    "skills/website-lead-enrichment/... . Repo-relative paths matched nothing there, so",
    "typecheck in an installed copy failed with TS18003 'No inputs were found'.",
    "One glob is correct in both layouts."
  ],
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "out"]
}
